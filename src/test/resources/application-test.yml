# Challenge 5: Database Configuration for Tests
# 
# This configuration is incomplete. The candidate needs to:
# 1. Add H2 dependency to pom.xml
# 2. Configure H2 datasource settings below
# 3. Set up proper JPA configuration for H2
# 4. Create data.sql or schema.sql for test data

spring:
  profiles:
    active: test
    
  # TODO: Configure H2 database for testing
  # Candidate needs to add proper H2 configuration here
  # datasource:
  #   url: jdbc:h2:mem:testdb
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password: 
    
  # jpa:
  #   hibernate:
  #     ddl-auto: create-drop
  #   show-sql: true
  #   database-platform: org.hibernate.dialect.H2Dialect
    
  # h2:
  #   console:
  #     enabled: true

logging:
  level:
    com.interview: DEBUG
    org.springframework.web: DEBUG
