# Challenge 1: Database Configuration for Tests
# FIXED: Complete H2 configuration for testing

spring:
  # FIXED: H2 database configuration for testing
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    
  h2:
    console:
      enabled: true

logging:
  level:
    com.interview: DEBUG
    org.springframework.web: DEBUG
